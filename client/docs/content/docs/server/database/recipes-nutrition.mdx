---
title: Recipes & Nutrition
description: Database tables representing ingredients, portions, recipes, and user food tracking logs.
---

# Recipes & Nutrition

This module represents the primary nutrition records, recipe formulations, raw ingredient properties, and daily calorie consumption logs.

Migrated by: [00003_create_recipes_and_nutrition.sql](file:///c:/Users/300079/CapCode/ForkFit/server/db/migrations/00003_create_recipes_and_nutrition.sql)

---

## 1. Ingredients Catalog

### `ingredients`
Defines base food items and their macronutrient distributions per 100 grams.

* **Schema**:
  | Column | Type | Constraints | Default | Description |
  | :--- | :--- | :--- | :--- | :--- |
  | `id` | `uuid` | `PRIMARY KEY` | `gen_random_uuid()` | Ingredient identifier. |
  | `name` | `text` | `NOT NULL UNIQUE` | - | Descriptive food name. |
  | `description` | `text` | - | - | Ingredient details. |
  | `calories_per_100g` | `numeric(6,2)` | `NOT NULL` | - | Calorie count per 100g. |
  | `protein_per_100g` | `numeric(5,2)` | `NOT NULL` | - | Protein content (g) per 100g. |
  | `carbs_per_100g` | `numeric(5,2)` | `NOT NULL` | - | Carbohydrates content (g) per 100g. |
  | `fat_per_100g` | `numeric(5,2)` | `NOT NULL` | - | Fat content (g) per 100g. |
  | `fiber_per_100g` | `numeric(5,2)` | `NOT NULL` | `0.00` | Dietary fiber (g) per 100g. |
  | `sodium_mg_per_100g` | `numeric(10,2)` | `NOT NULL` | `0.00` | Sodium level (mg) per 100g. |
  | `micronutrients` | `jsonb` | `NOT NULL` | `'{}'` | Key-value store for vitamins and minerals. |
  | `estimated_cost_per_100g` | `numeric(6,2)` | `NOT NULL` | `0.00` | Estimated cost (utilized by the Budget Agent). |
  | `price_currency` | `text` | `NOT NULL` | `'INR'` | Price currency code. |
  | `barcode` | `text` | - | - | UPC/EAN barcode string. |
  | `is_verified` | `boolean` | `NOT NULL` | `false` | True if verified by nutrition experts. |
  | `food_code` | `text` | `UNIQUE` | - | Internal/standard system code. |
  | `primary_source` | `text` | - | - | Origin source registry (e.g. `'Anuvaad INDB'`). |
  | `created_at` | `timestamptz` | `NOT NULL` | `now()` | Record creation timestamp. |
  | `updated_at` | `timestamptz` | `NOT NULL` | `now()` | Record change timestamp. |

### `ingredient_portions`
Specifies portion equivalence factors to convert serving units (e.g., cups, slices) into grams.

* **Schema**: `id` (PRIMARY KEY), `ingredient_id` (foreign key ingredients), `serving_unit` (e.g. `'cup'`), `grams_equivalent` (checking `> 0`), `created_at`, `updated_at`. Joint unique index on `(ingredient_id, serving_unit)`.

---

## 2. Recipe Structure

### `recipes`
Header metadata for food recipes.

* **Schema**:
  | Column | Type | Constraints | Default | Description |
  | :--- | :--- | :--- | :--- | :--- |
  | `id` | `uuid` | `PRIMARY KEY` | `gen_random_uuid()` | Unique recipe identifier. |
  | `owner_id` | `uuid` | `REFERENCES users(id) ON DELETE SET NULL` | - | User owner. Null indicates a public system recipe. |
  | `parent_recipe_id` | `uuid` | `REFERENCES recipes(id) ON DELETE SET NULL` | - | Reference to parent recipe (for alternatives/variations). |
  | `title` | `text` | `NOT NULL` | - | Recipe title. |
  | `description` | `text` | - | - | Recipe brief description. |
  | `instructions` | `text[]` | `NOT NULL` | - | Array list of recipe execution steps. |
  | `prep_time_minutes` | `integer` | - | - | Preparation time in minutes. |
  | `cook_time_minutes` | `integer` | - | - | Cooking duration in minutes. |
  | `servings` | `numeric(4,2)` | `NOT NULL` | `1.00` | Default servings yield. |
  | `cuisine` | `text` | - | - | Regional classification (e.g. `'North Indian'`). |
  | `dietary_tags` | `text[]` | `NOT NULL` | `'{}'` | Dietary categories (e.g. `{'Vegan', 'Gluten-Free'}`). |
  | `is_public` | `boolean` | `NOT NULL` | `false` | True if visible to other platform users. |

### `recipe_ingredients`
Junction table tracking recipe ingredient requirements.

* **Schema**: `recipe_id` (foreign key recipes), `ingredient_id` (foreign key ingredients), `quantity` (numeric), `unit` (e.g. `'g'`), `grams_equivalent` (computed weight), `notes`. Joint primary key on `(recipe_id, ingredient_id)`.

---

## 3. Daily Food Logging

### `food_logs`
Saves historical nutrient intakes consumed by users.

* **Schema**:
  | Column | Type | Constraints | Default | Description |
  | :--- | :--- | :--- | :--- | :--- |
  | `id` | `uuid` | `PRIMARY KEY` | `gen_random_uuid()` | Log identifier. |
  | `user_id` | `uuid` | `NOT NULL` | - | Foreign key referencing [users.id](file:///c:/Users/300079/CapCode/ForkFit/server/db/migrations/00001_create_iam.sql#L9) (ON DELETE CASCADE). |
  | `logged_at` | `timestamptz` | `NOT NULL` | `now()` | Date and time consumed. |
  | `meal_type` | `text` | `NOT NULL` | - | Meal segment: `'breakfast'`, `'lunch'`, `'dinner'`, or `'snack'`. |
  | `recipe_id` | `uuid` | `REFERENCES recipes(id) ON DELETE SET NULL` | - | Referencing recipe if logged as a whole dish. |
  | `ingredient_id` | `uuid` | `REFERENCES ingredients(id) ON DELETE SET NULL` | - | Referencing ingredient if logged individually. |
  | `custom_food_name` | `text` | - | - | Fallback text label if item not in catalog. |
  | `quantity` | `numeric(6,2)` | `NOT NULL` | - | Logged quantity. |
  | `unit` | `text` | `NOT NULL` | - | Unit (e.g. `'servings'`, `'grams'`). |
  | `calories` | `numeric(6,2)` | `NOT NULL` | - | Denormalized calorie total at logging time. |
  | `protein` | `numeric(5,2)` | `NOT NULL` | - | Denormalized protein summary (g). |
  | `carbs` | `numeric(5,2)` | `NOT NULL` | - | Denormalized carbohydrate summary (g). |
  | `fats` | `numeric(5,2)` | `NOT NULL` | - | Denormalized fat summary (g). |
  | `micronutrients_snapshot` | `jsonb` | `NOT NULL` | `'{}'` | Snapshot of micro nutrients at entry time. |
  | `created_at` | `timestamptz` | `NOT NULL` | `now()` | Log creation timestamp. |
